<% include partials/header %>
<h2>SOP to create this Demo</h2>

<ol>
        
    <li>Create Top Level Directory</li>
    <li>Create subdirecotoes public and views + views/partials</li>
    <li>npm init</li>
    <li>npm install request --save</li>
    <li>npm install express --save</li>
    <li>npm install body-parser --save (allows to work on request's body for POST routes)</li>
    <li>create in 'partials' header.ejs and footer.ejs</li>
    <li>create basic app.js and the first landing page</li>
    <li>add &lt;name&gt;.css file</name></li>
    <li>define more routes and corresponding (ejs) or res.sends - app.[get|post]("/&lt;something or :something&gt;, function (req, res){});</li>
    
</ol>

<h2>Movie API</h2>

<p>If a search option invoked and multiple results returned, there is an option to itereate through the returned results.</p>
	&lt;&#37; data[&quot;Search&quot;].forEach(function(movie){ &#37;&gt;<br/>
	    &lt;li&gt;&lt;&#37;= movie[&quot;Title&quot;] &#37;&gt;&lt;/li&gt;<br/>
		&lt;&#37; })&#37;&gt;

<% include partials/footer %>